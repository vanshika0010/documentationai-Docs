---
title: "QC Orders via API"
description: "Developer API — authentication, rate limits, webhooks, sandbox, API users, token management"
---

## Do we need to upload QC catalog if creating a QC order via API

No catalog needs to be uploaded if QC orders are getting created via API — it gets created from the API payload.